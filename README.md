# Problem 4: Three Ways Sum N

This project provides three different implementations to sum numbers from 1 to N.

## Running Tests

To run the tests, follow these steps:

1. Install the dependencies:

    ```sh
    npm install
    ```

2. Run the tests:

    ```sh
    npm test
    ```s